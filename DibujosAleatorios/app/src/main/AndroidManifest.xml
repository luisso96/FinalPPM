<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.luis.dibujosaleatorios">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DibujarAleatorioActivity" />
        <activity android:name=".DibujarPulsarActivity" />
        <activity android:name=".SpinnerFigurasActivity" />
        <activity
            android:name=".TransicionAnimacionActivity"
            android:label="@string/title_activity_transicion" >
        </activity>
    </application>

</manifest>